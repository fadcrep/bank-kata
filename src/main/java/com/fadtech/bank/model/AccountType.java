package com.fadtech.bank.model;

/**
 * The Enum AccountType.
 * description: AccountType is an enum that represents the type of account
 */

public enum AccountType {
    SAVINGS,
    CURRENT,
    OTHER
}
